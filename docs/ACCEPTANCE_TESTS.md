# Acceptance Tests

| Area | Automated evidence | Manual acceptance result |
| --- | --- | --- |
| Authentication and ownership | tRPC authorization and ownership tests | Sign in as a regular user and confirm admin routes are denied. |
| Resume and profile readiness | resume, profile-evidence, and profile-skill tests | Upload a supported resume, select its version, and verify readiness changes. |
| Discovery and safety | scraper, normalization, deduplication, freshness, and listing-safety tests | Run only configured ready sources; verify Jobicy and Arbeitnow documented API contracts, strict Arbeitnow remote-only filtering and attribution, and durable hourly polling limits; confirm blocked listings cannot be prepared. |
| Review and handoff | application approval, submission evidence, and autonomous execution-guard tests | Approve a prepared record and confirm the UI directs the user to complete the employer handoff. |
| Interview/offer tracking | response, interview notification, and success-fee tests | Record an employer response with source evidence and confirm interview notification behavior. |
| Privacy export | `server/privacyData.test.ts` | Verified locally through Settings: versioned export downloaded without private file bytes, credential fields, or a resume file URL. |
| Privacy deletion review | `server/privacyDeletionReview.test.ts`; `client/src/lib/adminReviewEvidence.test.ts` | Verified locally through Settings: request, no-deletion status, cancellation, and closed status; resolving in persistent mode creates a non-destructive execution plan. |
| Privacy retention and erasure | `server/privacyRetention.test.ts`; `server/privacyErasurePlanning.integration.test.ts`; `client/src/lib/privacyErasureControl.test.ts` | Classifies all 37 direct/indirect tables and fails closed on policy drift. Clean MySQL verifies idempotent planning, external cleanup, manual evidence, exact confirmations, lease exclusion, transactional rollback/retry, deletion, scrubbing, regulated-record preservation, pseudonymization, and idempotent completion. Workspace ownership blocks erasure while another active member depends on the owner. |
| Workspace governance | `server/workspaces.test.ts`; `client/src/lib/workspaceGovernanceWiring.test.ts` | Verifies authorization concealment, email-bound and expiring invitations, idempotent acceptance, role ceilings, ownership transfer, safe archival, bounded workloads, audit redaction, Team routing, and the candidate-data isolation boundary. |
| Connector revocation | `server/connectorRevocation.test.ts`; `server/connectorDisconnect.test.ts`; `server/connectorAccounts.test.ts` | Disconnect disables access first; scoped Google/Dropbox/GitHub revocation removes local grants, Google cleans both services, Microsoft/LinkedIn return manual guidance, and provider failure retains encrypted retry evidence without exposing tokens. |
| Admin denial | `client/src/lib/adminQueryAuthorization.test.ts` | A regular user receives Access Denied without firing privileged admin queries or producing new authorization errors. |
| Runtime operations | `server/_core/httpSafety.test.ts`; `npm.cmd run doctor` | Verified locally: `/healthz` and `/readyz` returned 200; production doctor fails with incomplete configuration. |
| Operational failure monitoring | `server/operationalFailureLog.test.ts`; `client/src/lib/adminOperationalFailureWiring.test.ts`; authenticated browser QA | Known server failures aggregate into a cardinality-bounded, process-local redacted ledger. Only administrators can query it, the admin view refreshes every 30 seconds, unknown labels collapse without retaining supplied text, and upstream exception objects are never accepted. |
| Runtime schema alignment | `server/databaseSchemaAudit.test.ts`; `npm.cmd run db:audit-schema`; container CI | The production bundle compares all Drizzle tables, SQL column types, nullability, primary and named indexes, ordered index columns, uniqueness, foreign-key targets, and delete/update actions with migrated MySQL metadata after applying the complete history, then fails on any drift. |
| Windows runtime | `server/_core/network.test.ts`; `server/windowsCiWorkflow.test.ts`; `scripts/check-windows-runtime.ps1`; `windows-latest` CI | The native launcher builds, audits configuration, binds explicitly, and reports success only after `/healthz` responds. CI parses both PowerShell launchers and verifies network/HAI contracts and the production build; credential-complete launch remains operator acceptance. |
| HAI status connector | `server/haiConnector.test.ts`; `server/haiConnectorPerformance.test.ts` | Verified locally on port 3040: Agent Card discovery, concealed unauthenticated status, and authenticated A2A 1.0 aggregate status completed without exposing user content or executing actions. Status counts use bounded database aggregates; runtime health exposes only totals and the latest timestamp, while individual failure labels remain administrator-only. |
| Inbox response discovery efficiency | `server/inboxResponseDiscovery.test.ts`; `server/inboxResponseDiscoveryDataMinimization.test.ts`; `server/employerResponseBatchLookup.test.ts` | A bounded Gmail or Outlook scan loads only owned application ID/status and job company/title, then checks all matched provider references with one indexed, user/source-scoped database lookup. Resume, cover-letter, notes, salary, and URL fields stay outside the scanner; duplicate inputs collapse and cross-account records cannot suppress a candidate. |
| Exact connector account access | `server/connectorAccountExactLookup.test.ts`; `server/connectorAccountExactLookupWiring.test.ts`; focused provider suites | Inbox discovery/confirmation, follow-up mail, cloud documents, GitHub, LinkedIn, and OAuth start/callback paths retrieve one account through the unique `(user_id, provider)` path. Missing and adjacent provider records remain isolated; multi-provider dashboards retain the list API intentionally. |
| ngrok exposure | `scripts/start-ngrok.ps1`; ngrok configuration check | ngrok 3.39.8 and its local configuration were verified. Public health and callback acceptance remain pending a reserved HTTPS hostname. |
| Production bundle | `scripts/check-production-bundle.mjs`; `npm.cmd run build` | Production HTML must remain below 25 KiB and exclude Manus/JSX-location development instrumentation. Current shell: 487 bytes, down from 367,750 bytes. |
| Supply-chain advisories | `server/supplyChainCi.test.ts`; `pnpm security:audit` | The pnpm 11 lockfile must resolve all patched transitive paths, permit only esbuild dependency scripts, and make CI reject every moderate, high, or critical registry advisory. |
| Dashboard request efficiency | `client/src/lib/dashboardPerformanceClaims.test.ts`; `server/applicationCampaigns.test.ts` | Dashboard uses one operating-snapshot query, returns at most 10 projected recent applications, and does not create campaign state during a protected read. |
| Browser shell accessibility | `client/src/lib/dashboardLayoutAccessibility.test.ts`; authenticated Playwright dashboard/review-queue smoke | Shared routed pages expose one primary landmark; brand navigation and the account menu have keyboard-accessible names; dashboard-to-review navigation passes without console warnings; and the dashboard fits a 390-pixel viewport without horizontal overflow. Independent accessibility review remains release acceptance. |
| Application ledger scale | `server/applicationPagination.test.ts`; `server/applicationCampaigns.test.ts`; `client/src/lib/dashboardPerformanceClaims.test.ts`; `server/migrationJournal.test.ts` | The primary ledger API returns at most 100 records per request; dashboard and autonomous cycles use the oldest 250 active records, exact current-job history and daily preparation counts, exact aggregate totals, bounded approval/review queues, and explicit truncation. Synthetic 260-application and 105-approval workloads pass. Representative MySQL-volume load and query-plan inspection remain deployment acceptance. |
| Embedded development runtime | `server/_core/viteConfig.test.ts` | Build and serve modes share the same factory; development keeps its client root/instrumentation while production excludes it. `/src/main.tsx` returned JavaScript in the local smoke. |
| Operating query indexes | `server/migrationJournal.test.ts` | Migrations `0036`-`0037` and the Drizzle schema must declare the same 18 operating indexes. Production acceptance must apply the migrations and inspect query plans against representative data. |
| Database recovery | `server/databaseRecovery.test.ts`; `scripts/database-backup.mjs`; `scripts/database-restore.mjs` | Backup streams through local or explicitly named Docker MySQL clients, publishes only a complete checksummed bundle, and excludes credentials. A two-container MySQL 8.4 drill verified 60,071 bytes, SHA-256 integrity, restore into an isolated target, and matching 40-table/39-migration/sentinel state. Repeat against deployment storage before launch. |
| Discovery traffic controls | `server/discoveryTrafficPolicy.test.ts`; `server/scrapers/baseScraper.test.ts`; `server/scrapers/scraperManager.test.ts`; `server/scrapers/jobicyScraper.test.ts`; `server/scrapers/arbeitnowScraper.test.ts` | Every network adapter propagates cancellation and shared HTTP handling. Per-source runs are serialized, provider-specific polling limits survive restarts, cross-source concurrency and deadlines are bounded, timeouts abort fetches, transient retries honor bounded backoff and `Retry-After`, permanent failures are not retried, and production doctor requires an explicit source allowlist. |
| Container and migration runtime | `server/containerPackaging.test.ts`; `server/migrationJournal.test.ts`; CI `container-runtime` | Build with pnpm 11 and workspace overrides, reject missing production configuration, apply the complete 48-entry migration history under an advisory lock to empty MySQL 8.4, start non-root without Vite, become Docker-healthy, return production database readiness, and serve the frontend. |
| Operating evidence batching | `client/src/lib/dashboardPerformanceClaims.test.ts`; `server/responseInterviewMemory.test.ts` | Responses, schedules, and follow-ups are loaded through three shared batch paths, mixed application IDs return only owned records, and interview preparation uses one user-level lookup. |
| Preloaded offer attribution | `server/applicationApprovals.test.ts`; `client/src/lib/dashboardPerformanceClaims.test.ts` | The operating ledger reuses its existing application, approval, and response data; mixed-user supplied rows are filtered to the authenticated user. |
| Scoped admin snapshot | `server/auditAdminReview.test.ts`; `client/src/lib/dashboardPerformanceClaims.test.ts` | Admin operating-ledger reads are capped at 100 active items for the requested user and cannot include another user's or closed review records. |
| Scoped verification review | `server/auditAdminReview.test.ts`; `server/adminVerificationReview.test.ts` | Employment-verification decisions load and resolve only active reviews for the affected user and verification without reading the global admin queue. |
| Scoped application approvals | `server/applicationApprovals.test.ts`; `client/src/lib/dashboardPerformanceClaims.test.ts` | Standalone lifecycle and admin evidence reads include only approvals linked to the affected user's application, including legacy application-entity records. |
| Exact admin evidence | `server/applicationDecisions.test.ts`; `server/devReviewQueueSeed.test.ts`; `client/src/lib/dashboardPerformanceClaims.test.ts` | Review evidence retrieves one owned application and one user/job decision, preserves cross-owner isolation, and avoids full application or decision history reads. |
| Exact lifecycle ownership | `server/applicationApprovals.test.ts`; `server/applicationWithdrawal.test.ts`; `server/responseInterviewMemory.test.ts`; `server/followUpsMemory.test.ts`; `client/src/lib/dashboardPerformanceClaims.test.ts` | Single-application lifecycle and approval mutations authorize through exact owned records without scanning user-wide application or approval collections. |
| Exact preparation idempotency | `server/applicationDecisions.test.ts`; `server/applicationAutomationRoute.test.ts`; `client/src/lib/dashboardPerformanceClaims.test.ts` | Create, decision, and portal-preparation routes canonicalize the job and retrieve only its existing pending owned application through the unique user/job path. |
| Scoped decision close | `server/applicationDecisions.test.ts`; `client/src/lib/dashboardPerformanceClaims.test.ts` | Save/ignore decisions close one prepared application and its submission approval through exact user/job and application-scoped reads while retaining cancellation and audit evidence. |
| Scoped employer reply target | `server/followUpsMemory.test.ts`; `client/src/lib/dashboardPerformanceClaims.test.ts` | Reply drafting retrieves only the explicit owned response or newest owned replyable response and rejects a cross-user response ID. |
| Bounded interview preparation context | `server/interviewPrepPolicy.test.ts`; `server/responseInterviewMemory.test.ts`; `client/src/lib/dashboardPerformanceClaims.test.ts` | Preparation uses the earliest future scheduled/rescheduled interview for one owned application without loading its schedule history. |
| Batched autonomous follow-up evidence | `server/autonomousSubmissionApprovals.test.ts`; `server/autonomousOrchestrator.test.ts`; `client/src/lib/dashboardPerformanceClaims.test.ts` | One autonomous run reuses three ownership-scoped evidence batches across all follow-up safety and cooldown decisions. |
| Due job-alert processing | `server/applicationJobAlerts.test.ts`; `server/scrapers/scheduler.test.ts`; `server/migrationJournal.test.ts` | Idle cycles stop after the indexed due-alert query; active cycles page the complete current canonical-job set, stop comparing an alert after its first match, bulk-refresh internal matches, and send no external notifications. |
| Account localization | `server/accountLocale.test.ts`; `server/localizationContract.test.ts`; `client/src/lib/localizationWiring.test.ts`; `server/migrationJournal.test.ts` | Supported regional locales normalize safely, unsupported API values fail validation, account updates remain owner-scoped and audited, and English/Dutch shell language follows persisted state. |

Expected outcomes are enforced through tests where practical. Provider callbacks, real S3 objects, Stripe webhook delivery, and legal/compliance flows require credentials and controlled external verification.
